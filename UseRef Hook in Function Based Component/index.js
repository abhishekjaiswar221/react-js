//Don't use everytime use when necessary it affects the performance of the application
import {createRoot} from "react-dom/client";
import App from "./App";

createRoot(document.getElementById("root")).render(<App/>);
