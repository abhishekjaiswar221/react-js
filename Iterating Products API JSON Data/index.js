//Using the Products API JSON Data in useState() Hook and Iterating the data and styling the data and displaying in a tabular format.
import {createRoot} from "react-dom/client";
import App from "./App";

createRoot(document.querySelector("#root")).render(<App/>);